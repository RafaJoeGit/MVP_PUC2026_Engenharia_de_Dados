# Camada Bronze

## Tabela

`workspace.default.bronze_listings`

A camada Bronze representa a primeira camada persistida do pipeline. Seu
objetivo é manter os dados recebidos da fonte em uma camada de
referência antes dos tratamentos utilizados nas etapas posteriores.

A Bronze preserva a rastreabilidade da origem e separa a entrada dos
dados das transformações analíticas.
